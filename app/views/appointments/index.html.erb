<p id="notice"><%= notice %></p>

<h1>Appointments</h1>

<% start_date = params.fetch(:start_date, Date.today).to_date %>
<% date_range = (start_date.beginning_of_month.beginning_of_week..start_date.end_of_month.end_of_week).to_a %>

<% appointments_in_this_month = @appointments.select{ |app| ( date_range.include?(app.start_time.to_date))} %>

<div class="month">      
  <ul>
    <li class="prev" ><%= link_to "<", start_date: (date_range.first - 1.day).beginning_of_month %></li>
    <li class="next"><%= link_to ">", start_date: (date_range.last + 1.day).beginning_of_month %></li>
    <li>
      <%= start_date.strftime("%B")%>
      <span style="font-size:18px"><%= start_date.year %></span>
    </li>
  </ul>
</div>


<ul class="weekdays">
<li>Mon</li>
    <li>Tue</li>
    <li>Wed</li>
    <li>Thu</li>
    <li>Fri</li>
    <li>Sat</li>
    <li>Sun</li>
</ul>

<% appointment_days = [] %>
<% appointments_in_this_month.each do |app| appointment_days.push(app.start_time.to_date) end%>

<ul class="days">
  <% date_range.each_slice(7) do |week| %>
    <% week.each do |day| %>
      <% if day_index = appointment_days.index(day) %>
        <li><div><%= appointments_in_this_month[day_index].name %><br> <%= day.day%></div></li>
      <% elsif day == start_date %>
        <li><span class="active"><%= day.day %></span></li>
      <% else %>
        <li><div><%= day.day %></div></li>
      <% end %>
    <% end %>
  <% end %>
</ul>
  



<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Start time</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @appointments.each do |appointment| %>
      <tr>
        <td><%= appointment.name %></td>
        <td><%= appointment.start_time %></td>
        <td><%= link_to 'Show', appointment %></td>
        <td><%= link_to 'Edit', edit_appointment_path(appointment) %></td>
        <td><%= link_to 'Destroy', appointment, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Appointment', new_appointment_path %>
